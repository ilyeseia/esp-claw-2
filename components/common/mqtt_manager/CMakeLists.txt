idf_component_register(
    SRCS
        "mqtt_manager.c"
    INCLUDE_DIRS
        "include"
    REQUIRES
        mqtt
        esp_event
        esp_timer
        esp_hw_support
        freertos
    PRIV_REQUIRES
        esp-tls
        mbedtls
)
