# دليل خدمة SSH على ESP-Claw (`cap_ssh`)

دليل مرجعي كامل لخدمة SSH المُضافة إلى `edge_agent` — من التفعيل إلى الاستخدام اليومي واستكشاف الأخطاء، مبني بالكامل على تجربة تطوير واختبار حقيقية على جهاز ESP32-S3 فعلي.

---

## 1. نظرة عامة

`cap_ssh` هو خادم SSH صغير مبني على مكتبة **wolfSSH**، يعمل مباشرة على الجهاز، ويمنحك وصولاً تفاعليًا آمنًا لاستدعاء أي "قدرة" (capability) مسجّلة في النظام — نفس القدرات التي يستخدمها الوكيل الذكي (agent) والكونسول التسلسلي.

**هذا ليس بديلاً عن الكونسول الكامل** (أوامر مثل `ask`/`session`/`auto` غير متاحة عبر SSH). إنه shell صغير ومُخصَّص بأربعة أوامر فقط، مصمم خصيصًا لتوزيع (dispatch) القدرات عن بُعد بأمان.

### لماذا لا يُستخدم esp_console الكامل؟
لأن إعادة توجيه `stdin`/`stdout` الخاص بـ `esp_console` لكل جلسة SSH قد يُفسد سجل الكونسول التسلسلي الفعلي إن كان أحدهم متصلاً به في نفس الوقت — وهذا خلل معروف في ESP-IDF. بدلاً من ذلك، يستدعي `cap_ssh` نفس الدوال الأساسية (`claw_cap_call`, `claw_cap_build_catalog`) مباشرة.

---

## 2. نموذج الأمان (اقرأ هذا أولاً)

| الخاصية | القيمة |
|---|---|
| المصادقة | **بالمفتاح العام فقط** — لا كلمات مرور إطلاقًا |
| عدد المفاتيح المُصرَّح بها | **مفتاح عميل واحد فقط** حاليًا |
| الواجهة | يستمع على **كل الواجهات** (0.0.0.0) — نفس نموذج واجهة الويب المحلية |
| مستوى الثقة داخل الـ shell | **مطابق تمامًا للوصول الفيزيائي عبر المنفذ التسلسلي** (`CLAW_CAP_CALLER_CONSOLE`) |

**النقطة الأهم**: بمجرد نجاح مصافحة SSH (إثبات امتلاك المفتاح الخاص)، فإن أي أمر `call <name> <json>` يعمل **بصلاحيات كاملة بلا أي قيود إضافية** — يشمل ذلك أدوات `ROOT_AGENT_ONLY`/`RESTRICTED` مثل `ssh_configure` نفسها أو `ota_update` أو `wireguard_configure`. هذا **مقصود ومطابق** لنموذج ثقة المنفذ التسلسلي: من يملك مفتاحًا خاصًا صالحًا = من يملك وصولاً فيزيائيًا للجهاز، بنفس المستوى تمامًا.

> **قارن هذا** بجسر MCP (`cap_mcp_bridge`) الذي يعمل بمستوى ثقة أقل بكثير (`SUB_AGENT`) لأن خادم MCP **بلا أي مصادقة إطلاقًا** — بينما SSH يُصادق فعليًا، فلا حاجة لتقييد إضافي بعد المصافحة.

---

## 3. القيد الأهم: نوع المفتاح — RSA أو ECDSA فقط، **وليس ed25519**

هذا اكتُشف عمليًا أثناء التطوير وليس افتراضًا نظريًا: عند استخدام مفتاح `ssh-ed25519`، ترفضه مكتبة wolfSSH **بصمت تمامًا** قبل أن تصل المصادقة حتى إلى كود الجهاز — لا رسالة خطأ واضحة، فقط "Permission denied" عادي يبدو وكأنه مفتاح خاطئ.

**السبب التقني**: هذا البناء من wolfSSL يُعرّف `HAVE_ED25519` (لمكتبة التشفير الأساسية wolfCrypt)، لكن **ليس** الماكروهات الثلاثة الإضافية التي تحتاجها wolfSSH نفسها لتفعيل ed25519 في *مصادقة المستخدم* تحديدًا:
- `WOLFSSL_ED25519_STREAMING_VERIFY`
- `HAVE_ED25519_KEY_IMPORT`
- `HAVE_ED25519_KEY_EXPORT`

بدون هذه الثلاثة، يُعرّف `wolfssh/internal.h` تلقائيًا `WOLFSSH_NO_ED25519`، فتصبح "ssh-ed25519" نوع مفتاح "غير معروف" تمامًا بالنسبة لـ wolfSSH.

**الحل**: استخدم دائمًا:
```bash
ssh-keygen -t ecdsa -b 256 -f clientkey
```
أو `-t rsa` إن أردت. **لا تستخدم أبدًا** `-t ed25519`.

---

## 4. التفعيل (Kconfig)

يتطلب تفعيل خيارين في `menuconfig` (كلاهما معطّل افتراضيًا لأنهما يُدخلان تبعية تشفير/SSH جديدة):

| الخيار | الموقع | القيمة الافتراضية |
|---|---|---|
| `CONFIG_APP_CLAW_CAP_SSH` | App Claw Capabilities | `n` |
| `CONFIG_ESP_ENABLE_WOLFSSH` | wolfSSL Component Config → wolfSSH | `n` |

بعد تفعيلهما، يحتاج المشروع إعادة بناء كاملة (تغييرات Kconfig تُعيد بناء آلاف الملفات).

---

## 5. الإعداد من واجهة الويب (الطريقة الموصى بها)

اذهب إلى **إعدادات النظام ← SSH** في واجهة الويب الخاصة بالجهاز.

### الحقول:

| الحقل | الاسم التقني | الوصف |
|---|---|---|
| تفعيل خادم SSH | `ssh_enabled` | `"true"`/`"false"` — يشغّل الخادم عند الإقلاع |
| المفتاح الخاص للمضيف | `ssh_host_private_key_der_b64` | مفتاح هوية SSH **الخاص بالجهاز نفسه**، بصيغة DER مُرمَّزة base64 |
| المفتاح العام المُصرَّح به | `ssh_authorized_public_key` | سطر OpenSSH عام واحد فقط — **العميل الوحيد** المسموح له بالاتصال |

### خطوات التوليد الكاملة (من جهازك أنت، وليس من الجهاز المُضمَّن):

**أ) مفتاح هوية المضيف** (يُولَّد مرة واحدة فقط، يمثّل "هوية" الجهاز نفسه):
```bash
ssh-keygen -m PEM -t ecdsa -f hostkey -N ""
openssl ec -in hostkey -outform DER -out hostkey.der
```
ثم حوّله إلى base64 (سطر واحد بلا فواصل أسطر):
```bash
# Linux/macOS/Git-Bash:
base64 -w0 hostkey.der
# أو عبر PowerShell:
[Convert]::ToBase64String([IO.File]::ReadAllBytes("hostkey.der"))
```
انسخ الناتج بالكامل والصقه في حقل "المفتاح الخاص للمضيف".

> **لماذا DER وليس PEM؟** لأن `wolfSSH_CTX_UsePrivateKey_buffer()` مع صيغة `WOLFSSH_FORMAT_PEM` أرجعت خطأ `WS_UNIMPLEMENTED_E` (-1017) في اختبار حي على هذا البناء — غير مُنفَّذة فعليًا رغم وجودها في الواجهة البرمجية. DER يعمل بشكل صحيح ومُتحقَّق منه.

**ب) مفتاح العميل** (هذا الذي تستخدمه أنت للاتصال):
```bash
ssh-keygen -t ecdsa -b 256 -f clientkey
```
سيُنشئ `clientkey` (خاص — **لا تشاركه أبدًا مع أحد، ولا حتى معي**) و`clientkey.pub` (عام — آمن للمشاركة).

اعرض المفتاح العام:
```bash
# Linux/macOS:
cat clientkey.pub
# Windows:
type clientkey.pub
```
انسخ السطر بالكامل (يبدأ بـ `ecdsa-sha2-nistp256 AAAA...`) والصقه في حقل "المفتاح العام المُصرَّح به".

**ج) احفظ ثم أعد تشغيل الجهاز** — تغيير المفاتيح على خادم يعمل بالفعل يتطلب إعادة تشغيل ليصبح ساري المفعول (قيد v1 معروف).

---

## 6. الإعداد عبر واجهة برمجية (API) مباشرة

يمكن إعداد نفس الحقول عبر `POST /api/config` على الجهاز مباشرة (نفس ما تفعله واجهة الويب داخليًا):

```bash
curl -X POST "http://<device-ip>/api/config" \
  -H "Content-Type: application/json" \
  -d '{
    "ssh_enabled": "true",
    "ssh_host_private_key_der_b64": "<base64 DER>",
    "ssh_authorized_public_key": "ecdsa-sha2-nistp256 AAAA... comment"
  }'

curl -X POST "http://<device-ip>/api/restart"
```

للقراءة (يعرض المفاتيح كنص صريح — لا يوجد إخفاء حاليًا لهذه الحقول، تحقّق أنك على شبكة موثوقة):
```bash
curl "http://<device-ip>/api/config?groups=ssh"
```

---

## 7. الإعداد عبر أداة `claw_cap` مباشرة (كونسول تسلسلي، أو وكيل بصلاحية root)

توجد أداتان مسجّلتان في claw_cap:

### `ssh_configure` (مقيّدة، root-agent فقط، **غير متاحة عبر MQTT**)
```json
{
  "host_private_key_der_b64": "<base64 DER>",
  "authorized_public_key": "ecdsa-sha2-nistp256 AAAA... comment"
}
```
على الكونسول التسلسلي، بما أن محلّل الأوامر (`esp_console_split_argv`) يُقسّم المدخل عند أي مسافة غير محميّة، يجب تغليف الـ JSON كاملاً بعلامات اقتباس مزدوجة مع تهريب (`escape`) كل علامة اقتباس وكل شرطة مائلة عكسية داخله:
```
cap call ssh_configure "{\"host_private_key_der_b64\":\"...\",\"authorized_public_key\":\"ecdsa-sha2-nistp256 AAAA... comment\"}"
```

### `ssh_status` (متاحة للجميع، للقراءة فقط)
```
cap call ssh_status {}
```
يُرجع:
```json
{"enabled": true, "configured": true, "running": true, "port": 22}
```
**لا يُرجع المفاتيح أبدًا.**

---

## 8. استخدام الـ shell بعد الاتصال

```bash
ssh -i clientkey root@<device-ip>
```
(اسم المستخدم `root` غير مهم فعليًا — المصادقة بالمفتاح فقط، بلا تحقق من اسم المستخدم).

عند أول اتصال، سيسألك SSH عن الثقة بمفتاح المضيف الجديد — أجب `yes`.

### الأوامر المتاحة داخل الـ shell:

| الأمر | الوظيفة |
|---|---|
| `help` | عرض قائمة الأوامر |
| `list` | عرض كل قدرات `claw_cap` المسجّلة (الاسم، المجموعة، الوصف) |
| `groups` | عرض مجموعات القدرات وحالتها (`registered`/`started`/إلخ) |
| `call <name> <json>` | استدعاء أي قدرة، مثال: `call get_current_time {}` |
| `exit` أو `quit` | إغلاق الجلسة بشكل نظيف وفوري |

### مثال جلسة كاملة:
```
$ ssh -i clientkey root@192.168.1.76
ESP-Claw SSH console
ESP-Claw SSH console. Type 'help'.
> call get_current_time {}
2026-09-18 08:13:36 CET (Friday)
> call get_system_info {}
{"chip_model":"esp32s3","memory":{...},"wifi":{...}, ...}
> exit
Goodbye.
Connection to 192.168.1.76 closed by remote host.
```

> **ملاحظة حول الصدى (echo)**: الجلسات التفاعلية الحقيقية (وليس عبر توجيه إدخال من ملف) تعرض الآن الحروف أثناء كتابتك، مع دعم Backspace. هذا أُضيف بعد اختبار حي أظهر أن الجلسة التفاعلية الأولى بدت "متجمّدة" رغم أنها كانت تعمل فعليًا — لم يكن هناك أي انعكاس بصري لما يُكتب.

---

## 9. الوصول عن بُعد عبر VPN (Tailscale أو WireGuard)

بما أن الخادم يستمع على **كل الواجهات**، فهو متاح تلقائيًا عبر أي نفق VPN فعّال دون أي إعداد إضافي على الجهاز:

- **وضع بوابة Tailscale**: يتطلب إعدادًا خارج هذا الجهاز — على جهاز الـ subnet-router: `tailscale up --advertise-routes=<lan-subnet>/24`، ثم اعتماد ذلك المسار في لوحة تحكم Tailscale. بعدها، أي عميل على نفس الـ tailnet مع "accept routes" مفعّلة يمكنه `ssh -i clientkey root@<device-lan-ip>` من أي مكان.
- **وضع WireGuard**: بعد إعداد `wireguard_configure` وتفعيل النفق عبر `vpn_connect`، اتصل بـ SSH على عنوان IP النفق بدلاً من عنوان الشبكة المحلية.

كلا الوضعين مُنفَّذان بالفعل في `cap_vpn` — لا كود إضافي مطلوب هنا.

---

## 10. استكشاف الأخطاء (كلها مشاكل حقيقية واجهناها أثناء التطوير)

| العرض | السبب الحقيقي | الحل |
|---|---|---|
| `Permission denied (publickey,password)` مع مفتاح ed25519 | wolfSSH يرفض ed25519 بصمت (انظر §3) | استخدم مفتاح ECDSA أو RSA |
| `Warning: Identity file ... not accessible` | استخدمت `-i` مع نص بدل مسار ملف | `-i` يجب أن يشير لمسار **ملف** المفتاح الخاص |
| اتصلت بمفتاح المضيف الخاص بالخطأ | الخلط بين "مفتاح هوية الجهاز" (host key) و"مفتاح اتصالك أنت" (client key) — هما شيئان مختلفان تمامًا | استخدم مفتاح العميل (`clientkey`) الذي أنشأته أنت، ليس `ssh_host_private_key_der_b64` |
| الجلسة التفاعلية تبدو متجمّدة، لا شيء يظهر عند الكتابة | غياب صدى الأحرف (echo) — تم إصلاحه | تأكد أنك تستخدم بناءً حديثًا يتضمن إصلاح echo (بعد الالتزام الذي يضيف `cap_ssh_send` لكل حرف مكتوب) |
| `WS_UNIMPLEMENTED_E (-1017)` عند تحميل مفتاح المضيف | استخدام PEM بدل DER لمفتاح المضيف | حوّل المفتاح لصيغة DER مُرمَّزة base64 (انظر §5-أ) |
| تغيير المفتاح لم يُطبَّق | خادم SSH يعمل بالفعل — لا يدعم إعادة التحميل الحي في v1 | أعد تشغيل الجهاز بعد أي تغيير في المفاتيح |
| الاتصال يفشل تمامًا، لا استجابة إطلاقًا | تحقق من الشبكة نفسها أولاً (ping للجهاز، والراوتر) قبل افتراض خلل في SSH | كان لدينا حالة فقدان اتصال شبكة محلية كاملة أثناء التطوير — لم تكن متعلقة بـ SSH إطلاقًا |

---

## 11. القيود المعروفة (v1، بتصميم واعٍ)

- **مفتاح عميل واحد فقط مُصرَّح به** — لا قائمة مفاتيح متعددة (لمشاركة الوصول مع أكثر من شخص).
- **لا حماية من محاولات الاتصال المتكررة** (rate-limiting/lockout) — مخاطرة منخفضة عمليًا لأن المصادقة بالمفتاح العام فقط (يصعب تخمينها)، لكنها غير موجودة.
- **جلسة واحدة في كل مرة**.
- **تغيير المفاتيح على خادم يعمل بالفعل يتطلب إعادة تشغيل** — لا تحديث حي.
- **لا دعم ed25519** (انظر §3 للسبب التقني والحل البديل).

---

## 12. الملفات ذات الصلة في الكود المصدري

| الملف | الدور |
|---|---|
| `components/claw_capabilities/cap_ssh/include/cap_ssh.h` | التوثيق الكامل للتصميم والقرارات التقنية |
| `components/claw_capabilities/cap_ssh/src/cap_ssh.c` | التنفيذ: الخادم، المصادقة، الـ shell |
| `application/edge_agent/components/app_config/include/app_config.h` | تعريف حقول `ssh_enabled`/`ssh_host_private_key_der_b64`/`ssh_authorized_public_key` |
| `components/common/app_claw/app_capabilities.c` | ربط الإعداد (`app_cap_prepare_ssh`/`app_cap_ssh_persist`) بين `app_config` وكاب `cap_ssh` |
| `application/edge_agent/components/http_server/frontend_source/src/pages/SshPage.tsx` | صفحة الويب |
